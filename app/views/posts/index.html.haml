.content.pure-u-1.pure-u-md-3-4       
  .posts
    %h1.content-subhead Recent Post
        
    %section.post
      - @post.each do |i|
        %header.post-header
        = image_tag i.image.url(:thumb),:class=>"post-avatar"
        %h2.post-title= link_to i.title,i,:class=>"linkto"
        %p.post-meta= i.body
      = will_paginate @post